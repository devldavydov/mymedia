{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "bot",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/cmd/mymediabot/",
            "args": [
                "-t", "BOT_TOKEN",
                "-u", "USER_ID",
                "-s", "./storage",
                "-b", "true"
            ]
        },
    ]
}